{
  "name": "worklog-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "npm install && node scripts/copy-wasm.mjs && mkdir -p public"
  },
  "dependencies": {
    "tesseract.js": "^5.0.5",
    "sharp": "^0.33.4"
  },
  "engines": {
    "node": ">=18"
  }
}