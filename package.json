{
  "name": "worklog-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm install && node scripts/copy-wasm.cjs && mkdir -p public"
  },
  "dependencies": {
    "tesseract.js": "^5.0.5",
    "sharp": "^0.33.4"
  },
  "engines": {
    "node": ">=18"
  }
}