{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": ".",
  "functions": {
    "api/webhook.js": {
      "maxDuration": 60
    }
  },
  "routes": [
    { "src": "/webhook", "dest": "/api/webhook.js" },
    { "src": "/", "dest": "/api/webhook.js" }
  ]
}